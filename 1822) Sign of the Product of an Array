class Solution(object):
    def arraySign(self, nums):
        def signFunc(x):
            if x>0:
                return 1
            elif x==0:
                return 0
            else:
                return -1
        p=1
        for i in nums:
            p*=i
        return signFunc(p)
